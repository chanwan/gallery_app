<div class="container">
  <div class="row">
  <% @images.each do |img| %>
    <div class="col-lg-3 col-md-4 col-xs-6 thumb">
      <a class="thumbnail" href="#">
        <%= image_tag img.picture.url(:medium), class: "img-responsive" %>
      </a>
      <div class="linkz">
        <%= img.name %>
        <p> By: <%= link_to img.user.name  %></p>
      </div>
    </div>
    <% end %>
  </div>
</div>
